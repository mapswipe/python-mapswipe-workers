
  // Groups contains only meta info about each group
  // stored under the groups's unique ID

  
  // Group tasks are accessible
  // and stored by group ID -> task Id



  // Group results are accessible
  // and stored by group ID -> task Id -> user Id -> result Id
